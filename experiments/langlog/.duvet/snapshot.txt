SPECIFICATION: [Langlog Language Specification](SPEC.md)
  SECTION: [LLG-CLI-01 Single-File Front End](#llg-cli-01-single-file-front-end)
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: The phase 1 front end MUST accept `langlog check <path>`.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: The phase 1 front end MUST treat `<path>` as a single source file.

  SECTION: [LLG-LEX-01 Comments And Token Spans](#llg-lex-01-comments-and-token-spans)
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: The lexer MUST ignore line comments beginning with `//`.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: The lexer MUST ignore block comments delimited by `/*` and `*/`.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: The lexer MUST support nested block comments.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: The lexer MUST report an error for an unterminated block comment.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: The lexer MUST attach a byte span to every emitted token.

  SECTION: [LLG-LEX-02 Identifiers And Literals](#llg-lex-02-identifiers-and-literals)
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: Identifiers MUST begin with an ASCII letter or `_` and MAY continue with ASCII
    TEXT[!MUST,implementation,test]:   letters, digits, or `_`.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: Integer literals MUST be parsed as unsigned base-10 integers.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: Boolean literals MUST include `true` and `false`.

  SECTION: [LLG-LEX-03 Reserved Keywords](#llg-lex-03-reserved-keywords)
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: The phase 1 keyword set MUST reserve `fn`, `let`, `mut`, `if`, `else`,
    TEXT[!MUST,implementation,test]:   `match`, `for`, `in`, `return`, `observe`, `true`, and `false`.

  SECTION: [LLG-SYN-01 Top-Level Functions](#llg-syn-01-top-level-functions)
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: A phase 1 source file MUST contain only function items at the top level.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: A function item MUST use Rust-like syntax with `fn`, a name, a parameter list,
    TEXT[!MUST,implementation,test]:   and a block body.
    TEXT[implementation]: The current parser allows the return type to be omitted in phase 1.

  SECTION: [LLG-SYN-02 Statements](#llg-syn-02-statements)
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: The parser MUST accept `let`, assignment, expression, `if`, `match`, `for`,
    TEXT[!MUST,implementation,test]:   `return`, and `observe` statements.
    TEXT[implementation]: The current parser allows a `let` statement to include `mut`, a type
    TEXT[implementation]:   annotation, and an initializer.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: A statement form that requires a semicolon MUST reject the form if the
    TEXT[!MUST,implementation,test]:   semicolon is absent.

  SECTION: [LLG-SYN-03 Expressions And Precedence](#llg-syn-03-expressions-and-precedence)
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: The parser MUST accept integer literals, boolean literals, names, tuples,
    TEXT[!MUST,implementation,test]:   arrays, blocks, grouped expressions, unary operators, binary operators, calls,
    TEXT[!MUST,implementation,test]:   and indexing expressions.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: Postfix call and indexing MUST bind tighter than unary operators.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: Unary operators MUST bind tighter than multiplicative operators.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: Multiplicative operators MUST bind tighter than additive operators.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: Additive operators MUST bind tighter than comparison operators.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: Comparison operators MUST bind tighter than equality operators.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: Equality operators MUST bind tighter than logical and.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: Logical and MUST bind tighter than logical or.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: Logical or MUST bind tighter than range construction.

  SECTION: [LLG-TYPE-01 Phase 1 Types](#llg-type-01-phase-1-types)
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: The parser MUST accept unit, named, tuple, fixed-array, and generic
    TEXT[!MUST,implementation,test]:   application type forms.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: A fixed-array type MUST use the form `[T; N]`.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: `Set<T, N>` and `Map<K, V, N>` MUST carry explicit capacity arguments in the
    TEXT[!MUST,implementation,test]:   source type.

  SECTION: [LLG-DIAG-01 Source Spans And Syntax Diagnostics](#llg-diag-01-source-spans-and-syntax-diagnostics)
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: The front end MUST preserve byte spans for tokens and syntax nodes or derive
    TEXT[!MUST,implementation,test]:   them from spanned children without reparsing source text.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: Syntax diagnostics MUST include a primary source span.
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: The CLI MUST render syntax errors with file path, line, column, source line
    TEXT[!MUST,implementation,test]:   text, and an underline for the primary span.

  SECTION: [LLG-SEMA-01 Name Resolution And Scopes](#llg-sema-01-name-resolution-and-scopes)
    TEXT[!MUST]: -
    TEXT[!MUST,todo]: The semantic phase MUST resolve item, parameter, and local bindings according
    TEXT[!MUST,todo]:   to lexical scope.
    TEXT[!MUST]: -
    TEXT[!MUST,todo]: The semantic phase MUST reject references to undefined bindings.

  SECTION: [LLG-SEMA-02 Totality Constraints](#llg-sema-02-totality-constraints)
    TEXT[!MUST]: -
    TEXT[!MUST,todo]: The semantic phase MUST reject direct recursion.
    TEXT[!MUST]: -
    TEXT[!MUST,todo]: The semantic phase MUST reject indirect recursion.
    TEXT[!MUST]: -
    TEXT[!MUST,todo]: The semantic phase MUST reject unbounded iteration forms that are outside the
    TEXT[!MUST,todo]:   bounded phase 1 loop model.

  SECTION: [LLG-PROOF-01 Proof-Required Operations](#llg-proof-01-proof-required-operations)
    TEXT[!MUST]: -
    TEXT[!MUST,todo]: The proof phase MUST reject arithmetic that may overflow unless safety is
    TEXT[!MUST,todo]:   proven.
    TEXT[!MUST]: -
    TEXT[!MUST,todo]: The proof phase MUST reject division or remainder operations that may divide
    TEXT[!MUST,todo]:   by zero unless safety is proven.
    TEXT[!MUST]: -
    TEXT[!MUST,todo]: The proof phase MUST reject indexing that may go out of bounds unless safety
    TEXT[!MUST,todo]:   is proven.

  SECTION: [LLG-PROOF-02 Observations](#llg-proof-02-observations)
    TEXT[!MUST]: -
    TEXT[!MUST,todo]: The proof phase MUST derive facts from control-flow tests such as comparisons,
    TEXT[!MUST,todo]:   range checks, length checks, and membership tests.
    TEXT[!MUST]: -
    TEXT[!MUST,todo]: The proof phase MUST incorporate explicit `observe` statements into the fact
    TEXT[!MUST,todo]:   model.

  SECTION: [LLG-REL-01 Collections And Relations](#llg-rel-01-collections-and-relations)
    TEXT[!MUST]: -
    TEXT[!MUST,implementation,test]: The language MUST parse capacity-bounded `Set<T, N>` and `Map<K, V, N>` types.
    TEXT[!MUST]: -
    TEXT[!MUST,todo]: The first enforced relation MUST allow membership in a `Set<K, N>` to imply
    TEXT[!MUST,todo]:   presence in a `Map<K, V, M>`.
